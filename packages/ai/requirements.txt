# ── API server ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── Core ML / vision ──────────────────────────────────────────────────────────
ultralytics==8.2.0        # YOLOv8 detection + segmentation
pillow==10.3.0
numpy<2.0

# ── SAM 2 + Grounded SAM (optional — enables SAM2Model and GroundedSAMModel) ──
# Uncomment to enable these models in the AI panel.
# First use downloads ~185 MB (SAM 2) or ~750 MB (Grounded SAM) from HuggingFace.
transformers>=4.40.0,<5.0.0   # v5+ requires torch>=2.4; pin to v4 for torch 2.2 compat
accelerate>=0.20.0

# ── PyTorch — choose ONE of the three blocks below ───────────────────────────

# CPU-only (works everywhere, ~2x slower inference):
torch==2.3.0
torchvision==0.18.0

# CUDA 12.1 (NVIDIA GPU — comment out CPU block above, uncomment this):
# torch==2.3.0+cu121
# torchvision==0.18.0+cu121
# --extra-index-url https://download.pytorch.org/whl/cu121

# Apple Silicon MPS (M1/M2/M3 — comment out CPU block above, uncomment this):
# torch==2.3.0
# torchvision==0.18.0
# (MPS is included in the standard torch build for macOS)
